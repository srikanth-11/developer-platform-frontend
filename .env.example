# Committed template — copy to `.env` and adjust. (`.env` is git-ignored.)
# Vite only exposes vars prefixed with VITE_ to client code.

# Base URL of the backend API (NestJS global prefix is /api, default port 3333).
VITE_API_BASE_URL=http://localhost:3333/api
